# AGENTS.md

本文件是 `dingtalk_knowledge_governance` 的 AI 工程化约束。修改项目时先读本文、`README.md` 和 `docs/phase-1-plan.md`。

## 项目边界

- 本项目是独立的钉钉知识库治理服务，负责知识库/文档元数据同步、增量统计、AI 评审、人工复核与治理看板。
- 不替代钉钉知识库的编辑体验；一期不执行创建、修改、删除知识库或文档的写操作。
- 不修改其他子仓库。需要扩展 `bi_center` 内部契约时，先单独说明接口、影响与人工同步点。

## 数据与安全

- 不长期保存知识库正文、附件或导出文件；正文仅在评审任务执行期间存在于内存或容器 `tmpfs`，任务结束后立即清理。
- 数据库只保存节点元数据、内容指纹、评分、结构化问题、审计状态和必要的统计结果；默认不保存正文片段。
- 正文、附件、token、secret、Cookie、手机号和用户原始资料不得进入日志、错误响应、测试夹具或 Git。
- `.env`、数据库卷、Redis 数据、运行日志、临时文件和真实人员数据不得提交。

## 员工与组织

- `bi_center` 是员工、部门、业务组、负责人、身份策略和月度归属的唯一来源。
- 本服务通过 `bi_center` 内部 API 消费最终结果；不得直连 `bi_center_sync_db`，不得复制 7 部门、分部过滤、字段反转或身份猜测规则。
- 正式员工主键只使用 `employeeKey=UnionID`。UserID、工号、邮箱、姓名仅作为批量身份解析输入。
- 只有 `matched` 且 `includeInOfficialStats=true` 的身份进入正式员工/部门统计；其他结果进入诊断。
- 月度报表使用对应月度组织快照，不用当前部门覆盖历史归属。

## 技术与部署

- 目标栈：Python 3.11、FastAPI、MySQL、Redis、原生 HTML/CSS/JavaScript、ECharts。
- 使用 Docker Compose 编排 API、后台 worker 与 Redis；MySQL 默认使用受管或既有实例，开发环境可通过 profile 启动本地数据库。
- API 和 worker 对临时文档使用独立 `tmpfs`，不得挂载持久化文档目录。
- UI 视觉与交互参考 `bi_center` 和 `ai_code_review_web`：蓝灰色工作台、顶部栏、左侧导航、白底卡片、筛选表格、分数徽标和响应式窄屏布局。

## 评审与审计

- 每次评审生成不可变的 `review_instance_id`；重评新增记录，不覆盖历史记录。
- “重评次数”由同一文档的评审实例数量减一计算，不维护易漂移的手工计数器。
- 评分必须保存评分规则版本、模型配置版本、评审范围、维度分、理由和内容指纹。
- 无正文权限时只能输出“元数据合规分”，不得标记为完整内容质量分。
- AI 分数和人工审核结论分开保存，AI 不替代知识库审核员的最终责任。

## 验证

- 代码修改按风险运行单元测试、`compileall`、前端 `node --check` 和 Docker Compose 配置检查。
- 部署后检查 `docker compose ps`、`/api/health`、worker 状态、同步批次、评审队列和最近错误日志。
- 只改 Markdown 时，至少确认文件 UTF-8 可读、无真实密钥或人员数据，并检查 Git 状态。

